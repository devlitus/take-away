import { Injectable } from '@angular/core';
import { Jsonp, Headers } from "@angular/http";
import "rxjs/add/operator/map";

@Injectable()
export class BookService {

    constructor(private _service: Jsonp) { }

    
}